# P0.1 validation record

Date: 2026-07-19

## Passed

- `Backend`: `npm test` — 5/5 P0.1 authorization and route-regression tests passed.
- `Backend`: `node --check` passed for every repository JavaScript file outside `node_modules`.
- `Docs/OPENAPI.yaml`: parsed successfully with PyYAML; it contains one `components` section with security schemes, schemas, and responses.
- Static regressions: no executable `google-login`, `GoogleLogin`, legacy NGO approve/reject routes, or Google-login UI handler remains. NGO and FAQ mutation routes are server-side administrator-protected.

## Environment-limited checks

The sandbox cannot resolve `registry.npmjs.org` (`ENOTFOUND`). Both `npm ci` commands therefore stopped before dependencies were installed. The frontend `typecheck`, `lint`, and `build` commands cannot run in this sandbox because their local dependencies (including React, Vite, and ESLint) are unavailable.

The TypeScript configuration compatibility issue reported by the available TypeScript runner was corrected by removing obsolete `baseUrl` settings. A normal network-enabled CI environment should run:

```bash
cd Backend && npm ci && npm test
cd ../Frontend && npm ci && npm run typecheck && npm run lint && npm run build
```

No secrets, `.env` files, `node_modules`, or build caches are included in the delivered archive.
